export class Recipe {
    constructor(public id: string | null, public title: string | null, public shortDesc: string | null, public description: string | null, public imageUrl: string | null, public userId: string | null) {
    }
}